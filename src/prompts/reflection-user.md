Audit the following recent completed tasks and host observability snapshot.

Active model: {{active_model}}
Project: {{project}}
Mode: {{mode}}

## Tasks JSON

```json
{{tasks_json}}
```

## Observability JSON

```json
{{observability_json}}
```

Return 0–3 findings via the `respond` tool. Cite only `sourceEntryIds` present in the tasks JSON. Quote rates only when denominators are nonzero. Do not invent metrics.
